import React from 'react';
import Loader from 'semantic-ui-react/dist/commonjs/elements/Loader'

const AppLoader = () => {
  return <Loader active inline='centered' style={{marginTop: '5em'}}/>
}

export default AppLoader
